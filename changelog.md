# Changelog

### 1.2.0
- Phaser 3.90
- Phaser-sugar library 1.1.6
- storybook hot reload keeps same story with same url in browser
- storybook keeps all stories in own dir (recommended way
- npm run atlas - make atlas from src_assets/atlas
- npm run assets - generate src/autoAssetsConfig.ts with correct relative path
- all prod-ready assets are living in public/assets folder
- public folder like in big frontend
- src_assets folder for assets sources

#### 1.2.1 patch

- `free-tex-packer-core` target for  `0.3.4`, because `0.3.5` is log spamming
